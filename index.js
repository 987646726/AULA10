let produto = prompt (`Digite o preço de um produto`)

let cor = prompt (`Digite a cor da etiqueta`)

let desconto = 0

if (cor === 'Vermelho') {
    desconto 
} else if (cor === 'Vermelho') {
    console.log(`Desconto de 10`)
}